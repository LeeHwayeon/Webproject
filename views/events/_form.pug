.form-group
  label(for="title") Title
  input.form-control(type="text", name="title", placeholder="What's your event?", value=event.title)
.form-group
  label(for="location") Location
  input.form-control(type="text", name="location", placeholder="Where's the place?", value=event.location)  
.form-row
  .form-group-col-md-5
    label(for="when") Starts
    br
    input.form-control(type="date" id="userdate" name="stdate" value=event.stdate)
    br
    | Start Time
    br
    input(type="time" name="sttime" value=event.sttime)
  .form-group-col-md-5
    label(for="when") Ends
    br
    input.form-control(type="date" id="userdate" name="endate" value=event.endate)
    br
    | End Time
    br
    input(type="time" name="endtime" value=event.endtime)
br
.form-group
    input(type='checkbox' name='freeticket' value=event.freeticket) 
    | Free ticket
    input(type='checkbox' name='paidticket' value=event.paidticket) 
    | Paid ticket
    p
    | Ticket Price (paid ticket인 경우 입력)
    input(type="number" name="price" value=event.price)
.form-group
  label(for="event") Event Description
  textarea.form-control(name="content", rows="5")= event.content
.form-group(action="/action_page.php")
  label(for="file") 
  | Event Image
  input(type="file" name="image" multiple value=event.image)
.form-row
  .form-group-col-md-2
    label(for="organization") Organization Name
    input.form-control(type="text" name="organization" value=event.organization)
  .form-group-col-md-6
    label(for="organization") Organization Description
    input.form-control(rows="5", name="orcontent" value=event.orcontent)
br
.form-group
//- 티켓 값 받는거 구성 하기  
.from-group
  label(for="inputState") Event Type
  select(id="inputState", class="form-control" name="type" value=event.type)
    option Choose
    option Appearance or Signing
    option Attraction
    option Camp,Trip, or Retreat
    option Class, Training, or Workshop
    option Concert or Performance
    option Conference
    option Convention
    option Dinner or Gala
    option Festival or Fair
    option Game or Competition
    option Meeting or Networking Event
    option Other
    option Tour
br
.form-group
  label(for=topic) Event Topic
   select(id="inputState", class="form-control" name="topic" value=event.topic)
    option Business & Professional
    option Charity & Causes
    option Community & Culture
    option Family & Education
    option Fashion & Beauty
    option Film, Media & Entertainment
    option Food & Drink
    option Government & Politics
    option Health & Wellness
    option Hobbies & Special Interest
    option Home & Lifestyle
    option Music
    option Other